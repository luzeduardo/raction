{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "RAction",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceRoot}/${relativeFileDirname}/../target/debug/${relativeFileDirname}/../",
      "args": [],
      "cwd": "${workspaceRoot}",
    }
  ]
}